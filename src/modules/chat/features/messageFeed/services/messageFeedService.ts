import { MessageFeedData } from "../types";

export const getMessageFeed = async (): Promise<MessageFeedData> => {
  // TODO: Replace with Express API call — GET /api/v1/chat/messages
  return { messages: [] };
};
