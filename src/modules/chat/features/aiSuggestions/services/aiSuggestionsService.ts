import { AiSuggestionsData } from "../types";

export const getAiSuggestions = async (): Promise<AiSuggestionsData> => {
  // TODO: Replace with Express API call — GET /api/v1/chat/suggestions
  return { suggestions: [] };
};
